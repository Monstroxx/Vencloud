# Vencloud Railway Template
# Copy this to Railway Variables and customize the values

# Host and Port
HOST=0.0.0.0
PORT=8080

# Redis Configuration (Railway will auto-generate this)
REDIS_URI=${{Redis.REDIS_URL}}

# Root redirect URL
ROOT_REDIRECT=https://vencord.dev

# Discord OAuth Configuration
# Replace with your Discord Application values
DISCORD_CLIENT_ID=your_discord_client_id_here
DISCORD_CLIENT_SECRET=your_discord_client_secret_here
DISCORD_REDIRECT_URI=https://your-project.railway.app/v1/oauth/callback

# Security - Railway will auto-generate secure random strings (64 characters each)
PEPPER_SETTINGS=${{ secret(64) }}
PEPPER_SECRETS=${{ secret(64) }}

# Size limit for settings (in bytes, default: 1MB)
SIZE_LIMIT=1048576

# Allowed users (comma-separated Discord user IDs, leave empty for all users)
ALLOWED_USERS=

# Proxy header (if behind a reverse proxy)
PROXY_HEADER=X-Forwarded-For

# Prometheus metrics (true/false)
PROMETHEUS=false
